# Atlas runs locally and builds without environment variables or secrets.
#
# If a future server-side integration is added, document only the variable
# name and a non-secret description here. Store real values in the local or
# hosting secret manager. Never prefix a secret with NEXT_PUBLIC_.
